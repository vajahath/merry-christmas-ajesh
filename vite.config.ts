import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
    /**
     * For deploying to GitHub Pages as a subfolder, 
     * we must set the base path correctly.
     */
    base: '/merry-christmas-ajesh/',
});
